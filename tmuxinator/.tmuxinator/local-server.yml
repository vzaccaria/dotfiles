name: local-server
root: /Users/zaccaria

windows:
  - server:
      layout: main-vertical
      panes:
        - serverino
        - gotty --permit-arguments -p 3005 -w zsh 
        - /usr/local/bin/emacs --daemon
