name: edx-dockerino
root: /Users/zaccaria/development/github/org-edx/edx-dockerino

attach: false
post: tmux -CC attach


windows:
  - edx-dockerino:
      layout: main-vertical
      panes:
        - cd .
        - cd .
        - cd . && DEBUG=* ./index.js serve
