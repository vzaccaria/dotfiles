# ~/.tmuxinator/dot-emacs.yml

name: website
root: ~/development/github/vzaccaria.github.io

# Runs after everything. Use it to attach to tmux with custom options etc.
# post: tmux -CC attach -t dot-emacs

windows:
  - website:
      layout: main-vertical
      panes:
        - cd .
        - cd .
